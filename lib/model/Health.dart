class FootSteps{
  
  DateTime date;
  double count;

  FootSteps(this.date, this.count);
}

class ActiveMinutes{
  
  DateTime date;
  double minutes;

  ActiveMinutes(this.date, this.minutes);
}

class Distance{
  
  DateTime date;
  double distanceCount;

  Distance(this.date, this.distanceCount);
}

class Calories{
  
  DateTime date;
  double calorieCount;

  Calories(this.date, this.calorieCount);
}